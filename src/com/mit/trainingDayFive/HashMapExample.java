package com.mit.trainingDayFive;

public class HashMapExample {
//    // HashMap is a key-value pair
//
//    HashMap<String, String> studentNames = new HashMap();    // Initialize a HashMap, First String is the key, Second String is the value
//    // Cannot be int because int is a primitive data type, not a class
//
//    // Keys MUST be unique
//    studentNames.put("1","Archie");   // Key is "1", Value is "Archie"
//    studentNames.put("2","Jug Head");   // Key is "2", Value is "Jug Head"
//    studentNames.put("3","Betty");   // Key is "3", Value is "Betty"
//    studentNames.put("4","Veronica");   // Key is "4", Value is "Veronica"
//    studentNames.put(null, "Sabrina"); // Key is null, Value is "Sabrina" (Hash Map can hold null values for keys)
//
//    Set entrySet = studentNames.entrySet();  // Returns an entry set view of the map, which is a set of key-value pairs
//    Iterator entrySetIterator = entrySet.iterator();    // Returns an iterator over the elements in this set
//    Map<K, V>.Entry<K, V>;    // A map entry (key-value pair) (K is the key, V is the value)
//
//    Set<String> studentKeys = studentNames.keySet();    // Returns a set view of the keys contained in this map
//
//    for(
//    String key:studentKeys){  // For each key in studentKeys
//        System.out.println("Key: " + key + " Value: " + studentNames.get(key));   // Print the key and the value
//    }
//
//
//    Set<String> studentNameValues = (Set<String>) studentNames.values();  // Returns a collection view of the values contained in this map
//    Collection<String> strVal = studentNames.values(); // Returns a collection view of the values contained in this map
//
//    for(String val: strVal){
//        System.out.println("Value: " + val);
//    }
//
//
}
